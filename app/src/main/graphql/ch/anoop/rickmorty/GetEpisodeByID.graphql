query GetEpisodeByID($episodeId: ID!) {
    episode(id: $episodeId) {
        id
        name
        air_date
        episode
        created
        characters{
            id
            name
            image
        }
    }
}